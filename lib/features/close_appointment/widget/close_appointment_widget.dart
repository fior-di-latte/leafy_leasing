import 'package:leafy_leasing/shared/base.dart';

class CloseAppointmentWidget extends HookConsumerWidget {
  const CloseAppointmentWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext ctx, WidgetRef ref) {
    return const SizedBox();
  }
}
